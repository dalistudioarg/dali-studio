<section class="relative bg-slate-900 border-t border-slate-800 px-6 py-24 overflow-hidden">

  <!-- Glow sutil inferior -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_80%,rgba(99,102,241,0.08),transparent_60%)]"></div>

  <div class="relative z-10 max-w-4xl mx-auto text-center">

    <!-- Eyebrow -->
    <span class="text-xs uppercase tracking-[0.35em] text-slate-500">
      El problema
    </span>

    <!-- Frase principal -->
    <h2 class="mt-8 text-3xl md:text-5xl font-semibold leading-[1.2] text-white">
      La mayoría de las webs se diseñan
      <span class="block text-white/60">
        para verse bien.
      </span>

      <span class="block mt-6 text-indigo-400">
        No para generar resultados.
      </span>
    </h2>

    <!-- Refuerzo -->
    <p class="mt-10 text-slate-400 text-lg max-w-2xl mx-auto leading-relaxed">
      Se enfocan en colores, animaciones y tendencias…
      pero olvidan lo único que importa:
      <span class="text-white font-medium"> convertir visitas en clientes.</span>
    </p>

  </div>
</section>
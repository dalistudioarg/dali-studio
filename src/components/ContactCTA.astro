<section
  id="contacto"
  class="relative bg-slate-950 px-6 py-36 border-t border-slate-800 overflow-hidden"
>

  <!-- Glow sutil -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_0%,rgba(99,102,241,0.08),transparent_60%)]"></div>

  <div class="relative z-10 max-w-3xl mx-auto text-center">

    <h2 class="text-3xl md:text-5xl font-semibold text-white mb-6">
      ¿Empezamos tu proyecto?
    </h2>

    <p class="text-slate-400 max-w-xl mx-auto mb-20 text-lg leading-relaxed">
      Contame brevemente qué necesitás y te respondo en 24–48 hs con una propuesta clara.
    </p>

    <form class="space-y-8 text-left">

      <!-- Nombre -->
      <div>
        <label class="block text-sm text-slate-400 mb-2">Nombre</label>
        <input
          type="text"
          placeholder="Tu nombre"
          class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3
                 text-white placeholder-slate-500
                 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500
                 transition"
        />
      </div>

      <!-- Email -->
      <div>
        <label class="block text-sm text-slate-400 mb-2">Email</label>
        <input
          type="email"
          placeholder="tu@email.com"
          class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3
                 text-white placeholder-slate-500
                 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500
                 transition"
        />
      </div>

      <!-- Tipo -->
      <div>
        <label class="block text-sm text-slate-400 mb-2">
          Tipo de proyecto
        </label>
        <select
          class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3
                 text-white
                 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500
                 transition"
        >
          <option>Landing Base</option>
          <option>Landing Optimizada</option>
          <option>Web + Reservas Online</option>
          <option>No estoy seguro</option>
        </select>
      </div>

      <!-- Mensaje -->
      <div>
        <label class="block text-sm text-slate-400 mb-2">Mensaje</label>
        <textarea
          rows="5"
          placeholder="Contame sobre tu negocio, objetivos y tiempos..."
          class="w-full bg-slate-900 border border-slate-800 rounded-xl px-4 py-3
                 text-white placeholder-slate-500
                 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500
                 transition resize-none"
        ></textarea>
      </div>

      <!-- Botón -->
      <button
        type="submit"
        class="w-full bg-linear-to-r from-indigo-500 to-violet-500
               hover:from-indigo-600 hover:to-violet-600
               transition-all duration-300
               py-4 rounded-xl font-medium text-white
               shadow-[0_10px_30px_rgba(99,102,241,0.35)]
               hover:shadow-[0_15px_40px_rgba(99,102,241,0.5)]
               hover:-translate-y-0.5"
      >
        Enviar mensaje
      </button>

    </form>

    <p class="text-xs text-slate-500 mt-10 max-w-md mx-auto">
      Trabajo con pocos proyectos a la vez para asegurar calidad, foco y resultados reales.
    </p>

  </div>
</section>
<section id="hero" class="relative bg-slate-950  min-h-[92vh] flex items-center overflow-hidden px-6 py-32">

  <!-- Radial glow -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_35%,rgba(99,102,241,0.15),transparent_60%)]"></div>

  <div class="relative z-10 max-w-4xl mx-auto text-center">

    <!-- Eyebrow -->
    <span class="text-indigo-400/80 text-xs uppercase tracking-[0.35em]">
      — Desarrollo web estratégico
    </span>

    <!-- Headline -->
    <h1 class="mt-8 text-4xl md:text-6xl lg:text-7xl font-semibold leading-[1.1] text-white">
      Tu web no necesita verse
      <span class="block text-white/60">
        bonita.
      </span>

      <span class="block mt-6 text-indigo-400 drop-shadow-[0_0_25px_rgba(99,102,241,0.4)]">
        Necesita convertir.
      </span>
    </h1>

    <!-- Subheadline -->
    <p class="mt-10 text-slate-400 text-lg md:text-xl max-w-2xl mx-auto leading-relaxed">
      Diseño y desarrollo landing pages estratégicas para negocios que quieren
      atraer más clientes y posicionarse como marca seria.
    </p>

    <!-- CTA -->
    <div class="mt-12 flex flex-col sm:flex-row justify-center gap-4">

      <a
        href="#contacto"
        class="px-8 py-4 rounded-xl text-white font-medium
               bg-linear-to-r from-indigo-500 to-violet-500
               shadow-[0_10px_40px_rgba(99,102,241,0.35)]
               transition-all duration-300
               hover:shadow-[0_15px_50px_rgba(99,102,241,0.6)]
               hover:-translate-y-1">
        Empezar proyecto
      </a>

      <a
        href="#servicios"
        class="px-8 py-4 rounded-xl border border-slate-700 text-slate-300
               transition-all duration-300
               hover:bg-white/5 hover:border-slate-500 hover:text-white">
        Ver servicios
      </a>

    </div>
  </div>
</section>
